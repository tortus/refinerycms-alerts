<%= render 'refinery/admin/search_header', :url => refinery.alerts_admin_alerts_path %>
<div class='pagination_container'>
  <% if @alerts.any? %>
    <%= render 'alerts' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
